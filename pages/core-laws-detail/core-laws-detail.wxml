<view class="container">
  <view class="header">
    <view class="title">{{title}}</view>
    <view class="usage-tip">以下内容可以帮助您了解相关权益和维权方式</view>
    <view class="copy-bar">
      <button class="copy-all" bindtap="copyAllContent">
        <image src="/images/copy-icon.png" mode="aspectFit"></image>
        复制全部内容
      </button>
    </view>
  </view>

  <view class="content">
    <block wx:for="{{sections}}" wx:key="id">
      <view class="section">
        <view class="section-header">
          <view class="section-title">{{item.title}}</view>
          <image wx:if="{{item.title==='请求事项' || item.title==='法律依据'}}"
                 class="section-copy"
                 src="/images/copy-icon.png"
                 mode="aspectFit"
                 bindtap="copySection"
                 data-section="{{item}}"></image>
        </view>
        
        <view class="section-content">
          <block wx:for="{{item.content}}" wx:key="id">
              <view class="content-item">
                <text>{{item.text}}</text>
              </view>
          </block>
          
          <block wx:if="{{item.subsections}}">
            <view class="subsection" wx:for="{{item.subsections}}" wx:key="title">
              <view class="subsection-title">
                {{item.title}}
              </view>
              <block wx:for="{{item.content}}" wx:key="id">
              <view class="content-item">
                <text>{{item.text}}</text>
              </view>
              </block>
            </view>
          </block>
        </view>
      </view>
    </block>
  </view>

  <view class="help-hint">
    <view class="hint-text">
      <text>💡 温馨提示</text>
      <text>• 建议保存相关法条内容，以备维权时使用</text>
      <text>• 如需专业法律建议，请咨询律师或法律援助机构</text>
      <text>• 收集好相关证据材料，有助于维权成功</text>
    </view>
  </view>
</view>
